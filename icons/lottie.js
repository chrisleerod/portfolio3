var svgContainer = document.getElementById('ux');
var animItem = bodymovin.loadAnimation({
  wrapper: svgContainer,
  animType: 'svg',
  loop: true,
  path: 'icons/ux design.json'
});

var svgContainer = document.getElementById('int');
var animItem = bodymovin.loadAnimation({
  wrapper: svgContainer,
  animType: 'svg',
  loop: true,
  path: 'icons/data.json'
});

var svgContainer = document.getElementById('mot');
var animItem = bodymovin.loadAnimation({
  wrapper: svgContainer,
  animType: 'svg',
  loop: true,
  path: 'icons/motion-design2.json'
});

var svgContainer = document.getElementById('prod');
var animItem = bodymovin.loadAnimation({
  wrapper: svgContainer,
  animType: 'svg',
  loop: true,
  path: 'icons/product-design2.json'
});

var svgContainer = document.getElementById('web-des');
var animItem = bodymovin.loadAnimation({
  wrapper: svgContainer,
  animType: 'svg',
  loop: true,
  path: 'icons/web-design.json'
});

var svgContainer = document.getElementById('web-dev');
var animItem = bodymovin.loadAnimation({
  wrapper: svgContainer,
  animType: 'svg',
  loop: true,
  path: 'icons/web-development.json'
});